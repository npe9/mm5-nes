import XCTest
@testable import MMC5Dev
@testable import Shared

final class TestSuite: XCTestCase {
    func testAllSuites() async throws {
        // XCTest will automatically discover and run all test methods
        // in the test classes, so we don't need to call testAll explicitly
    }
}